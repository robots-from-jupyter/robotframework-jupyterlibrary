{
  "private": true,
  "prettier": {
    "proseWrap": "always",
    "singleQuote": true,
    "printWidth": 88
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  },
  "scripts": {
    "prettier": "prettier --write --list-different \"./*.{json,md,yml}\" \"{.github,.binder,docs}/**/*.{json,md,yml,css}\""
  }
}
